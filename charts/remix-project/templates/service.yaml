apiVersion: v1
kind: Service
metadata:
  name: {{ include "remix-project.fullname" . }}
  labels:
    {{- include "remix-project.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "remix-project.selectorLabels" . | nindent 4 }}
