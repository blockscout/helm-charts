{{- if .Values.userOpsIndexer.enabled }}
{{- if .Values.userOpsIndexer.ingress.enabled }}
{{- $fullName := include "blockscout-stack.fullname" . -}}
{{- $svcPort := .Values.userOpsIndexer.service.port -}}
{{- if and .Values.userOpsIndexer.ingress.className (not (semverCompare ">=1.18-0" .Capabilities.KubeVersion.GitVersion)) }}
  {{- if not (hasKey .Values.ingress.annotations "kubernetes.io/ingress.class") }}
  {{- $_ := set .Values.ingress.annotations "kubernetes.io/ingress.class" .Values.ingress.className}}
  {{- end }}
{{- end }}
{{- if semverCompare ">=1.19-0" .Capabilities.KubeVersion.GitVersion -}}
apiVersion: networking.k8s.io/v1
{{- else if semverCompare ">=1.14-0" .Capabilities.KubeVersion.GitVersion -}}
apiVersion: networking.k8s.io/v1beta1
{{- else -}}
apiVersion: extensions/v1beta1
{{- end }}
kind: Ingress
metadata:
  name: {{ $fullName }}-user-ops-indexer-ingress
  labels:
    {{- include "blockscout-stack.labels" . | nindent 4 }}
  {{- with .Values.userOpsIndexer.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if and .Values.userOpsIndexer.ingress.className (semverCompare ">=1.18-0" .Capabilities.KubeVersion.GitVersion) }}
  ingressClassName: {{ .Values.userOpsIndexer.ingress.className }}
  {{- end }}
  {{- if .Values.userOpsIndexer.ingress.tls.enabled }}
  tls:
    - hosts:
        - {{ .Values.userOpsIndexer.ingress.hostname | quote }}
      secretName: {{ .Values.userOpsIndexer.ingress.tls.secretName | default (printf "%s-user-ops-indexer-tls" $fullName) }}
  {{- end }}
  rules:
    - host: {{ .Values.userOpsIndexer.ingress.hostname | quote }}
      http:
        paths:
          {{- range .Values.userOpsIndexer.ingress.paths }}
          - path: {{ .path }}
            {{- if and .pathType (semverCompare ">=1.18-0" $.Capabilities.KubeVersion.GitVersion) }}
            pathType: {{ .pathType }}
            {{- else }}
            pathType: Prefix
            {{- end }}
            backend:
              {{- if semverCompare ">=1.19-0" $.Capabilities.KubeVersion.GitVersion }}
              service:
                name: {{ $fullName }}-user-ops-indexer-svc
                port:
                  number: {{ $svcPort }}
              {{- else }}
              serviceName: {{ $fullName }}-user-ops-indexer-svc
              servicePort: {{ $svcPort }}
              {{- end }}
          {{- end }}
{{- end }}
{{- end }}